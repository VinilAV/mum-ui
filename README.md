# Dashboard UI

Minimal React + Vite + Tailwind dashboard prototype.

Run locally:

```bash
npm install
npm run dev
```

Features included (mocked client-only):
- List environments, add environment
- Environment details with tabs: Overview, History, Health, Updates, Versions
- Add agent to environment
- Create & simulate update packages with live progress

(No tests included.)

